const express = require('express');
const app = express();
const port = 8080;

git add app.js
git commit -m "Full pipeline test version 2"

app.listen(port);
console.log(`App running on http://localhost:${port}`);
